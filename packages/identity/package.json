{
  "name": "@confer/identity",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "dev": "echo 'identity runs as library, no standalone dev server'",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@confer/shared": "workspace:*",
    "@noble/ed25519": "^2.2.0",
    "jose": "^6.0.0",
    "zod": "^3.24.0"
  }
}
