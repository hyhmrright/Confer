{
  "name": "@confer/gateway",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "test": "bun test",
    "test:db:setup": "bun run scripts/setup-test-db.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "eval:rag": "bun run src/eval/run-rag-eval.ts"
  },
  "dependencies": {
    "@confer/agent-runtime": "workspace:*",
    "@confer/identity": "workspace:*",
    "@confer/shared": "workspace:*",
    "argon2": "^0.45.1",
    "drizzle-orm": "^0.45.2",
    "exceljs": "^4.4.0",
    "hono": "^4.13.4",
    "jose": "^6.2.10",
    "mammoth": "^1.12.2",
    "minio": "^8.0.7",
    "pdf-parse": "^2.4.5",
    "postgres": "^3.4.9",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "drizzle-kit": "^0.31.10"
  }
}
